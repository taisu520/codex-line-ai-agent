{
  "name": "line-bot-ai-agent",
  "version": "0.2.0",
  "description": "LINE Bot AI Agent with LINE Messaging API and OpenAI chat replies.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "check": "node --check src/index.js && node --check src/commands.js && node --check src/openai-agent.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "openai": "^6.42.0"
  }
}
